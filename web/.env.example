# Copy to .env.local for local dev (npm run dev / npm run build reads it automatically).
# Never commit .env.local.

# Base URL of the backend API, including the /api/v1 prefix.
# Inlined into the client bundle at BUILD time (see README.md).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
